apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-secret-staging
  namespace: staging
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: postgres-secret-staging
  data:
  - secretKey: POSTGRES_USER
    remoteRef:
      key: family-heritage/staging
      property: POSTGRES_USER
  - secretKey: POSTGRES_DB
    remoteRef:
      key: family-heritage/staging
      property: POSTGRES_DB
  - secretKey: POSTGRES_PASSWORD
    remoteRef:
      key: family-heritage/staging
      property: POSTGRES_PASSWORD
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backend-secret-staging
  namespace: staging
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: backend-secret-staging
  data:
  - secretKey: APP_JWT_SECRET
    remoteRef:
      key: family-heritage/staging
      property: APP_JWT_SECRET
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-secret
  namespace: staging
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: ghcr-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: >-
          {"auths":{"ghcr.io":{"username":"{{ .username | toString }}","password":"{{ .password | toString }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  dataFrom:
    - extract:
        key: ghcr