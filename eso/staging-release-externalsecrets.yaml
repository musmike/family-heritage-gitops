apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-secret-staging-release
  namespace: staging-release
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: postgres-secret-staging-release
  data:
  - secretKey: POSTGRES_USER
    remoteRef:
      key: secret/family-heritage/staging-release
      property: POSTGRES_USER
  - secretKey: POSTGRES_DB
    remoteRef:
      key: secret/family-heritage/staging-release
      property: POSTGRES_DB
  - secretKey: POSTGRES_PASSWORD
    remoteRef:
      key: secret/family-heritage/staging-release
      property: POSTGRES_PASSWORD
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: backend-secret-staging-release
  namespace: staging-release
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: backend-secret-staging-release
  data:
  - secretKey: APP_JWT_SECRET
    remoteRef:
      key: secret/family-heritage/staging-release
      property: APP_JWT_SECRET
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ghcr-secret
  namespace: staging-release
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: ghcr-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: >-
          {"auths":{"ghcr.io":{"username":"{{ .username | toString }}","password":"{{ .password | toString }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  dataFrom:
    - extract:
        key: secret/ghcr