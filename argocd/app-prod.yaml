apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: family-heritage-prod
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: 'https://github.com/musmike/family-heritage-gitops.git'
      targetRevision: main
      path: envs/prod
      ref: values
      directory:
        include: '*.yaml'

    - repoURL: 'https://github.com/musmike/family-heritage-gitops.git'
      targetRevision: main
      path: charts/family-heritage
      helm:
        releaseName: prod
        valueFiles:
          - $values/envs/prod/values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true